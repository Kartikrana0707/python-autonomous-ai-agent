[project]
name = "ai-agent"
version = "0.1.0"
description = "This project was my effort to deep dive into the world of AI Agents and how they work behind the scenes !"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
